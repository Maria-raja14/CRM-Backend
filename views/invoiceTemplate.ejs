<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Invoice</title>
</head>
<body>
    <h1>Invoice #<%= invoice.invoicenumber %></h1>
    <p>Owner: <%= invoice.owner %></p>
    <p>Issue Date: <%= invoice.issueDate %></p>
    <p>Due Date: <%= invoice.dueDate %></p>
    <p>Status: <%= invoice.status %></p>
    <h3>Items</h3>
    <ul>
        <% invoice.items.forEach(item => { %>
            <li><%= item.deal %> - Quantity: <%= item.quantity %>, Price: <%= item.price %></li>
        <% }) %>
    </ul>
    <p>Total: <%= invoice.total %></p>
</body>
</html>
